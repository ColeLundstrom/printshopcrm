# Candidate drop-in for the existing nginx-fronted PrintShopCRM Pro service.
# Stage/review first; activation requires explicit deployment approval and a restart.
# Keep the current environment, service account, OAuth configuration and data paths intact.
[Service]
Environment=PSC_HOST=127.0.0.1
